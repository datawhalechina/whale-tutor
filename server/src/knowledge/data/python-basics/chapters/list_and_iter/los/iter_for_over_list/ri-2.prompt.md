## 求和

写一个函数 `sum_list(nums)`,返回 `nums` 中所有数字的和。

要求:
- 用 `for` 循环实现(不用内建的 `sum()`,我们想看你掌握循环)
- 空列表返回 `0`
- 单元素列表返回该元素

例如:

```python
sum_list([1, 2, 3])         # → 6
sum_list([])                # → 0
sum_list([-1, 1, -2, 2])    # → 0
```

把 `pass` 替换成你的实现,然后运行测试。
