正确答案是 **`a[2]`**。

- `a[0]` 是 'x'(首位)
- `a[1]` 是 'y'
- `a[2]` 是 'z'(尾位,因为 list 长度 3,索引 0/1/2)
- `a[3]` 会抛 `IndexError`(只有 3 个元素,合法索引最大是 2)
- `a[-2]` 是 'y'(从尾倒数第二,即 index 1)

获取尾元素也可以用 `a[-1]`,但本题让你区分**正索引**的边界。

经验法则:正索引上限 = `len(a) - 1`,**不是** `len(a)`。这是 Python(以及 C / Java)所有数组都成立的"零起源"约定。
