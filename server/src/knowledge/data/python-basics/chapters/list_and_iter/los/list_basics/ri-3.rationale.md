正确答案是 **B:合法,`len(a) == 4`**。

- list 元素无需同类型,`[1, 'two', 3.0, True]` 完全合法
- `len(a)` 返回**元素个数**,这里 4 个元素,所以是 4
- list **不会**因元素值相等而去重——`[1, True, 1]` 的 `len` 是 3 而不是 1。去重是 `set` 的行为
- 附注:Python 中 `True == 1` 且 `False == 0` 是事实,但这只影响相等判断,不影响 list 的长度。list 是**位置容器**,不是集合
