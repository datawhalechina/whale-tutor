**列表(list)** 是 Python 中最核心的可变序列容器,几乎任何业务代码都会用到。

本章覆盖 4 个学习目标(LO),从认识容器开始,逐步走到用 for 循环遍历列表完成实际任务。

学完这一章,你能够:

- 用多种方式创建列表,知道 list 与其他容器(tuple/dict/set)的区别
- 用索引和切片正确访问元素,理解负索引和切片的边界
- 区分"修改原列表"与"创建新列表",理解引用 vs 拷贝
- 用 for 循环遍历列表,完成累加/筛选/变换等常见任务
