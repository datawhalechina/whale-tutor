SQL 查询的基础是 SELECT 语句:**从某些表选某些列、用条件过滤、按某种顺序排**。

本章 2 个 LO,完整覆盖一个最小可用查询的 4 个子句:

- `SELECT` — 选哪些列(以及别名 / 计算列)
- `FROM` — 从哪个表
- `WHERE` — 用什么条件过滤行
- `ORDER BY` — 按什么排序

学完能写大多数日常 "我要看 X 表里满足 Y 条件的 Z 列" 的查询。
