# dependencies
node_modules
.pnpm-store

# build
dist
/build              # 仅根级 build/ (build-cli-bundle 中间产物);裸 `build` 会误伤 server/src/build/
*.tsbuildinfo

# env
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
Thumbs.db

# coverage / test
coverage
.nyc_output

# vite / nest cache
.vite
.cache

# 内部设计稿(给 maintainer 的草稿,非 OSS 内容)
notes

# Claude Code agent 会话历史(本地排查用,不入仓库)
.cc-history
